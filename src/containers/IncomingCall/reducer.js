import {} from './constants';

const initialState = {
};

export default function incomingCallReducer(state = initialState, action) {
    switch (action.type) {
    default:
        return state;
    }
}
